{
  "name": "windsurf-ask-continue",
  "displayName": "Windsurf Ask Continue",
  "description": "让 AI 对话永不结束 - 仅支持 Windsurf IDE",
  "version": "1.1.0",
  "publisher": "Rhongomiant1227",
  "author": {
    "name": "Rhongomiant1227",
    "url": "https://github.com/Rhongomiant1227"
  },
  "homepage": "https://github.com/Rhongomiant1227",
  "repository": {
    "type": "git",
    "url": "https://github.com/Rhongomiant1227/Windsurf_Ask_Continue"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "mcp",
    "ai",
    "windsurf",
    "continue",
    "infinite"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "askContinue.showStatus",
        "title": "Ask Continue: Show Status",
        "icon": "$(info)"
      },
      {
        "command": "askContinue.restart",
        "title": "Ask Continue: Restart Server",
        "icon": "$(refresh)"
      },
      {
        "command": "askContinue.openPanel",
        "title": "Ask Continue: Open Panel",
        "icon": "$(comment-discussion)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "askContinue",
          "title": "Ask Continue",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "views": {
      "askContinue": [
        {
          "type": "webview",
          "id": "askContinue.statusView",
          "name": "状态面板",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "configuration": {
      "title": "Ask Continue",
      "properties": {
        "askContinue.serverPort": {
          "type": "number",
          "default": 23983,
          "description": "Port for the Ask Continue HTTP server"
        },
        "askContinue.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start the server when Windsurf opens"
        },
        "askContinue.windowMode": {
          "type": "string",
          "enum": ["single", "multi"],
          "default": "single",
          "enumDescriptions": [
            "单窗口模式（强烈推荐）：更加稳定，失去焦点时也会弹出对话框，永久保留继续对话功能，不会因长时间未响应导致 MCP 无法发出对话",
            "多窗口模式：启用 409 焦点检查，确保多开对话时弹窗只在焦点窗口展开。但超过 30 秒未响应可能导致 Windsurf 取消 MCP 调用，无法发送对话"
          ],
          "description": "窗口模式选择：单窗口模式更稳定，多窗口模式适合多开 Windsurf 的场景"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0",
    "undici": "^5.28.4",
    "vsce": "^2.11.0"
  }
}
